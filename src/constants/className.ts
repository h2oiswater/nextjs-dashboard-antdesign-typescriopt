export enum API_CLASS_NAME {
    STORE = 'Store',
    NOTIFICATION = 'Notification',
    CATEGORY = 'Category',
    GOODS = 'Goods',
}

export enum DB_GOODS {
    CATEGORY = 'category'
}

export enum DB_STORE {
    LOGO = 'logo'
}

export enum DB_NAME {
    User = '_User'
}